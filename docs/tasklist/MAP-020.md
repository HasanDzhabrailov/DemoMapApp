# Tasklist - MAP-020

- [x] Read PRD
- [x] Read plan
- [x] Confirm scope is feature selection and info window flow only

- [x] Audit current feature selection flow in component and UI
- [x] Route renderer feature click handling through Store intent
- [x] Route info window dismiss through Store intent
- [x] Reuse `MapFeatureSelectionResolver` from Store-first flow
- [x] Reuse `MapFeatureInfoWindowStateMapper` from Store-first flow
- [x] Move feature lookup/orchestration into executor
- [x] Move resulting selection state transitions into reducer
- [x] Preserve conflicting overlay state rules if still required
- [x] Preserve ruler exclusion from shared info behavior

- [x] Confirm point click still opens info window
- [x] Confirm line click still opens info window
- [x] Confirm polygon click still opens info window
- [x] Confirm info window dismiss still works
- [x] Confirm opening map tools still clears shared info window if that is current behavior
- [x] Confirm ruler measurement still does not open shared info behavior

- [ ] Run `./gradlew :composeApp:compileDebugKotlinAndroid`
- [x] Run `./gradlew :composeApp:compileKotlinJvm`
- [ ] Run `./gradlew :composeApp:test`
- [x] Run `./gradlew :composeApp:compileTestKotlinJvm :composeApp:jvmTest --tests "ru.tech.demomapapp.feature.map.impl.DefaultMapScreenComponentTest" --tests "ru.tech.demomapapp.feature.map.impl.store.MapStoreExecutorTest" --tests "ru.tech.demomapapp.feature.map.impl.store.MapStoreReducerTest"`
- [x] Record that minimal relevant JVM verification was used because the ticket changed common/store logic only
- [x] Update this tasklist
- [x] Prepare concise diff summary
- [x] Prepare commit message

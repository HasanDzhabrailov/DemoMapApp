# Tasklist — MAP-007

- [x] Read PRD
- [x] Read plan
- [x] Confirm scope is limited to fixed center marker and center snapshot state
- [x] Confirm no geo-object creation flow is implemented in this ticket
- [x] Confirm renderer remains render-only
- [x] Add `lastCameraSnapshot` to main `MapScreenComponent.Model`
- [x] Add `onCenterMarkerClick()` to `MapScreenComponent`
- [x] Update `DefaultMapScreenComponent.onCameraIdle(...)` to store center snapshot in main state
- [x] Keep debug state separate from product state
- [x] Remove duplicate camera snapshot source from `DebugModel`
- [x] Add shared `CenterMarker` composable
- [x] Overlay the marker in `MapScreenContent` with `Alignment.Center`
- [x] Ensure the marker remains fixed while the map moves beneath it
- [x] Add center marker menu state to `MapScreenComponent.Model`
- [x] Open the menu from `onCenterMarkerClick()` in screen/component layer
- [x] Add shared overlay menu UI above the map renderer
- [x] Add `Создать точку` action in the screen UI layer
- [x] Use `lastCameraSnapshot` as the source for create-point selection
- [x] Close the menu after action selection
- [x] Close the menu when tapping outside the menu
- [x] Refine menu container to popup-based overlay for future menu growth
- [x] Keep create-point seam in screen/component layer only
- [x] Refactor temporary create-point seam to callback-based screen integration without storing action state in `Model`
- [x] Replace callback-only create seam with minimal map-feature create flow
- [x] Add minimal business state for created points in `MapState`
- [x] Create points from `lastCameraSnapshot` in component layer
- [x] Map business point state to neutral renderer input in mapper layer
- [x] Render created points through neutral `MapRenderModel` without product logic in renderer
- [x] Pass point title through neutral render model for map labeling
- [x] Render point title on the map without coupling renderer to business state directly
- [x] Replace immediate create after `Создать точку` with opening a bottom sheet on the screen layer
- [x] Add minimal create-flow state to `MapScreenComponent.Model`
- [x] Add `CreatePointDraft` with separate latitude/longitude/title inputs
- [x] Pre-fill draft latitude and longitude from `lastCameraSnapshot`
- [x] Route all create-form edits through `MapScreenComponent` events only
- [x] Add shared bottom sheet UI for point draft editing without business logic in composables
- [x] Show title preview text at the bottom of the create sheet
- [x] Create a point only from `onCreatePointConfirm()`
- [x] Keep draft-to-business conversion in component/use-case layer
- [x] Add automatic `createdAt` timestamp field to point business model
- [x] Assign `createdAt` only on create confirmation
- [x] Ensure no business logic is added to `MapRenderer`
- [x] Ensure no business logic is added to `MapLibreCameraAdapter`
- [x] Ensure existing rendering behavior remains unchanged
- [x] Run `./gradlew :composeApp:compileDebugKotlinAndroid`
- [x] Run `./gradlew :composeApp:compileKotlinJvm`
- [x] Run `./gradlew test`
- [x] Update this tasklist
- [x] Prepare concise diff summary
- [x] Prepare commit message

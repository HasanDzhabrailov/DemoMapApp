# Plan — MAP-009

## Summary
Remove the map debug panel and all related contract, state, and UI dependencies with the smallest possible change, while preserving `lastCameraSnapshot` and all existing map flows.

## Affected Modules
- `feature/map/api`
- `feature/map/impl`
- `feature/map/ui`

## File-Level Plan
- Remove `debugModel` from `MapScreenComponent`
- Remove `onDebugPanelToggle()` from `MapScreenComponent`
- Remove `DebugModel` from `MapScreenComponent`
- Remove debug-state storage from `DefaultMapScreenComponent`
- Remove debug toggle implementation from `DefaultMapScreenComponent`
- Remove debug panel subscription from `MapScreenContent`
- Remove debug panel rendering from `MapScreenContent`
- Update `PreviewMapScreenComponent`
- Delete `MapDebugPanel.kt`
- Verify no debug-related references remain

## Implementation Notes
- Keep `lastCameraSnapshot` unchanged
- Keep `onCameraIdle` behavior unchanged
- Keep `MapRenderer` untouched
- Keep point, line, polygon, and feature info flows untouched
- Prefer direct removal over refactoring

## Verification
- `./gradlew :composeApp:compileDebugKotlinAndroid`
- `./gradlew :composeApp:compileKotlinJvm`

## Task Breakdown
1. Re-check debug panel usage
2. Remove debug contract members
3. Remove debug state and toggle logic
4. Remove debug panel UI usage
5. Update preview implementation
6. Delete `MapDebugPanel.kt`
7. Search for remaining references
8. Run minimal verification

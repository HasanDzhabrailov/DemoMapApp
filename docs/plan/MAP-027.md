# Plan - MAP-027

## Summary
Refactor 3 UI-layer files by extracting focused classes and functions.

## Affected Modules
- `composeApp/src/androidMain/kotlin/ru/tech/demomapapp/feature/map/ui/`
- `composeApp/src/androidMain/kotlin/ru/tech/demomapapp/feature/map/location/` (new)
- `composeApp/src/commonMain/kotlin/ru/tech/demomapapp/feature/map/impl/`
- `composeApp/src/commonMain/kotlin/ru/tech/demomapapp/feature/map/ui/`

## Target Structure

### Phase 1: MapLocationEffectBinder
```
location/
├── AndroidLocationResolver.kt        (~60 lines)
│   - class AndroidCurrentLocationResolver
│   - suspend fun resolveCurrentLocation()
│
├── LocationPermissionRequester.kt   (~35 lines)
│   - class LocationPermissionRequester
│   - suspend fun requestLocationPermission()
│
├── MapLocationEffectBinder.android.kt (~50 lines)
│   - @Composable fun MapLocationEffectBinder()
│
└── LocationExt.android.kt           (~45 lines)
    - fun Context.hasPermission()
    - fun LocationManager.candidateProviders()
    - fun Location.isFreshEnough()
    - fun Location.toCoordinate()
```

### Phase 2: MapScreenContent
```
ui/
├── MapScreenContent.kt              (~70 lines)
│   - @Composable fun MapScreenContent()
│   - orchestrates MapRenderer + overlays
│
├── MapScreenOverlays.kt             (~80 lines)
│   - @Composable fun MapScreenOverlays()
│   - conditional overlay composition
│
├── MapScreenPreview.kt              (~100 lines)
│   - @Preview fun MapScreenContentPreview()
│   - class PreviewMapScreenComponent
│
└── MapScreenExtensions.kt           (~25 lines)
    - fun Model.isGpsToggleChecked()
    - fun Model.isCurrentLocationFocusEnabled()
    - fun Model.isManualLocationEnabled()
    - fun RenderFeatureClick.toFeatureType()
    - fun RenderFeatureClick.toFeatureInfoWindowAnchor()
```

### Phase 3: DefaultMapScreenComponent
```
impl/
├── DefaultMapScreenComponent.kt     (~110 lines)
│   - class DefaultMapScreenComponent
│   - constructor + model
│   - inline intent delegation
│
└── ComponentIntentDelegates.kt      (~90 lines)
    - inline fun MapScreenComponent.zoomIn()
    - inline fun MapScreenComponent.zoomOut()
    - ... (40 delegation helpers)
```

## Implementation Steps

### Phase 1: MapLocationEffectBinder Split
1. Create `location/AndroidLocationResolver.kt`
   - Extract `AndroidCurrentLocationResolver` class
   - Extract `resolveCurrentLocation()` method
   - Keep helper extensions internal
2. Create `location/LocationPermissionRequester.kt`
   - Extract `LocationPermissionRequester` class
   - Extract `rememberLocationPermissionRequester()`
3. Create `location/LocationExt.android.kt`
   - Move all extension functions
4. Thin `MapLocationEffectBinder.android.kt`
   - Keep only Composable function
   - Use extracted classes

### Phase 2: MapScreenContent Split
1. Create `ui/MapScreenOverlays.kt`
   - Extract overlay composition logic
   - Conditional rendering based on model state
2. Create `ui/MapScreenPreview.kt`
   - Extract Preview composable
   - Extract PreviewMapScreenComponent
3. Create `ui/MapScreenExtensions.kt`
   - Move all extension functions from MapScreenContent
4. Thin `MapScreenContent.kt`
   - Keep only main composable orchestration

### Phase 3: DefaultMapScreenComponent Refactor
1. Create `impl/ComponentIntentDelegates.kt`
   - Extract intent delegation helpers
   - Use inline functions to preserve call site
2. Reduce `DefaultMapScreenComponent.kt`
   - Keep constructor and model
   - Use delegation helpers

## Verification
- `./gradlew :composeApp:compileDebugKotlinAndroid`
- `./gradlew :composeApp:compileKotlinJvm`
- `./gradlew :composeApp:test`
- Manual: verify map UI renders correctly

## Risks and Mitigations
- Risk: Import explosion in files
  - Mitigation: use Kotlin file-level imports
- Risk: Extension delegation reduces readability
  - Mitigation: inline functions preserve call site clarity
- Risk: Preview extraction breaks preview mode
  - Mitigation: verify Preview annotation works

## Task Breakdown
1. Create AndroidLocationResolver.kt
2. Create LocationPermissionRequester.kt
3. Create LocationExt.android.kt
4. Update MapLocationEffectBinder.android.kt
5. Create MapScreenOverlays.kt
6. Create MapScreenPreview.kt
7. Create MapScreenExtensions.kt
8. Update MapScreenContent.kt
9. Create ComponentIntentDelegates.kt
10. Update DefaultMapScreenComponent.kt
11. Run verification commands
12. Manual UI verification
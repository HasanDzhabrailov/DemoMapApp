# Plan - MAP-024

## Summary
Finalize the map feature's MVIKotlin migration and codify the resulting responsibility-first architecture in `AGENTS.md`. This ticket is both a consolidation step and a guardrail step.

## Affected Modules
- `feature/map/api`
- `feature/map/impl`
- `feature/map/ui`
- `feature/map/render`
- `AGENTS.md`
- related docs only as needed

## Architecture Target
Each screen/feature should follow:

Component (Decompose)
- lifecycle
- Store holder
- navigation/screen bridge

Store (MVIKotlin)
- Intent
- State
- Label
- Reducer
- Executor

UI (Compose)
- render only
- send intents only

Renderer (platform-specific)
- rendering only
- platform integration only

## AGENTS.md Update Plan
Add/strengthen rules for:
- responsibility-first design
- split by ownership/change reason, not line count
- mandatory screen architecture with Decompose + MVIKotlin
- reducer purity
- executor side-effect ownership
- UI business-logic prohibition
- anti-pattern list
- new feature workflow starting from intent and Store

## Consolidation Strategy
1. Audit remaining map feature responsibility violations
2. Fix any small residual fake-MVI issues justified by current scope
3. Update `AGENTS.md` rules to reflect the final target
4. Ensure docs align with the migrated architecture
5. Verify behavior is unchanged

## Verification
- `./gradlew :composeApp:compileDebugKotlinAndroid`
- `./gradlew :composeApp:compileKotlinJvm`
- `./gradlew :composeApp:test`

## Risks and Mitigations
- Risk: "finalization" expands into another broad refactor
  - Mitigation: focus on residual cleanup and rule codification only
- Risk: AGENTS rules become too abstract
  - Mitigation: write concrete enforceable rules and anti-patterns
- Risk: residual compatibility code remains confusing
  - Mitigation: document any intentional leftovers clearly

## Task Breakdown
1. Audit remaining fake-MVI/responsibility issues
2. Fix small justified residual issues
3. Update `AGENTS.md` with responsibility-first MVIKotlin rules
4. Align docs with final architecture
5. Verify behavior remains unchanged
6. Run minimal verification

# Plan — MAP-003

## Summary
Separate map engine rendering concerns from feature and root layers while preserving current behavior and Decompose ownership.

## Affected Modules
- `feature/map/api`
- `feature/map/impl`
- `feature/map/ui`
- `root/ui`
- `androidMain` map renderer actual
- `jvmMain` map renderer actual

## Implementation Notes
- Change map screen UI contract to accept `MapScreenComponent`
- Keep `RootContent` responsible only for child selection/unwrapping
- Move renderer boundary out of `feature.map.api`
- Keep render engine contracts distinct from feature state
- Remove renderer mapping from composable body
- Consolidate default map style ownership in one place
- Preserve root-driven component lifecycle and current behavior

## Risks
- Feature state may currently encode renderer assumptions
- Package moves may affect expect/actual structure
- Small API changes may ripple through preview/test code

## Verification
- `./gradlew test`
- `./gradlew :composeApp:compileKotlinJvm`
- `./gradlew :composeApp:compileDebugKotlinAndroid`

## Task Breakdown
1. Decouple feature UI from `RootComponent.Child.MapScreen`
2. Move renderer contract to a render/engine boundary
3. Make feature map state library-agnostic
4. Remove renderer adaptation from composable UI
5. Consolidate default style ownership
6. Verify Android and JVM compilation/tests
